{
    "api_token": "",
    "instances": [{% for item in array %}
        {
            "label": "{{ item["name"] }}",
            "type": "{{ item["parameters"]["type"] }}",
            "region": "{{ item["parameters"]["region"] }}",
            "ansible_playbook_path": "{{ item["server"]["absolute_path"] }}/playbook.yml",
            "ssh_public_key": ""
        }{% if not loop.last %},{% endif %}{% endfor %}
    ]
}